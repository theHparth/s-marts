# Pending doc updates — not yet written into steps.html / architecture.html / tech pages / interview.html

Scratch tracking file only. Cleared out each time a full docs pass runs (user says "update docs" or similar).
Not linked from any page's sidebar, not part of the polished site.

---

## Phase 6 (Go Ingestion Service) — full pre-study docs pass written 2026-08-14
- `steps.html` Steps 41-52 (all Planned), `tech/go.html` (new page), 4 Q&A entries on `interview.html`, Go
  column on `tech/comparison.html` — all written from the planned Step 41-52 chat batch, per the
  phase-start-pre-study cadence. `architecture.html`'s `goingest` node and phase table row were already
  flipped to in-progress before the chat batch went out.
- **Still pending:** once the user actually builds Steps 41-52 for real and reports progress/errors/questions,
  do the lighter follow-up pass — flip "Planned" → "Done" per step, correct any commands/output that differed
  from the plan, fold in real questions asked while building into go.html + interview.html, flip
  architecture.html's goingest node to green.
